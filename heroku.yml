build:
  docker:
    web:
      context: ./front-end
      dockerfile: Dockerfile
    worker:
      context: ./back-end
      dockerfile: Dockerfile

  config:
    JVM_BUILD_TASK: './back-end/gradlew build'

release:
  image: web
  command:
    - java -jar app.jar
